import React from 'react'
import { AppContainer, BooksContainer } from '../../containers'

const BooksPage = () => (
  <AppContainer>
    <BooksContainer />
  </AppContainer>
)

export default BooksPage
